# Roadmap

- [ ] Make `/liveoutput` controls reliably hide after five seconds and return on interaction.
- [ ] Add a compact Playlist tab for saved scenes, timing, fades, ordering, and looping.
- [ ] Add a simple Live output mode selector: Current selection, Playlist, Hidden, Test pattern.
- [ ] Publish playlist playback data so the kiosk continues without the studio open.
- [ ] Verify editor controls and `/liveoutput` playback on desktop and mobile widths.
